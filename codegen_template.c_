/// header
#include <stdio.h>
#define MEM_SIZE (16 * 1024)

int main()
{
	int mem[MEM_SIZE];
	size_t idx;

/// ptr
	idx = (idx + $repetitions$) % MEM_SIZE;

/// val
	mem[idx] += $repetitions$;

/// loop_beg
	while(mem[idx])
	{

/// loop_end
	}

/// footer
	return 0;
}
