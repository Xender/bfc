/// header
#include <stdio.h>
#define MEM_SIZE (16 * 1024)

int main()
{
	char mem[MEM_SIZE] = {};
	size_t idx = 0;

/// ptr
	idx = (idx + $repetitions$) % MEM_SIZE;

/// val
	mem[idx] += $repetitions$;

/// in
	mem[idx] = getchar();

/// out
	putchar(mem[idx]);

/// loop_beg
	while(mem[idx]) {

/// loop_end
	}

/// footer
	return 0;
}
